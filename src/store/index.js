import { createStore } from "vuex";

// createStore({}) 创建仓库
export default createStore({
  state: {
    // 存放数据
  },
  mutations: {
    // 改数据函数
  },
  actions: {
    // 请求数据函数
  },
  modules: {
    // 分模块
  },
  getters: {
    // vuex 的计算属性
  },
});
